const msg = 'asdasd'
msg()

const user = {
    name: '1',
    age: 2
}
user.localtion()

const str = '123132'
str.toLocaleLowercase()
str.toLocaleLowerCase()

function random (){
    return Math.random < 0.5
}


const value = Math.random() < 0.5 ? 'a' : 'b'
if(value !== 'a'){

}
else if(value === 'b'){
    
}
